/**
 * @desc Component that returns a Spinner for the guide/event
 */

import React from "react";
import Spinner from "../../../Spinner/Spinner";

const SpinnerGuide = () => {
  // it just forwards the spinner for admin
  return <Spinner />;
};

export default SpinnerGuide;
